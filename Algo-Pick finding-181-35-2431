#include <stdio.h>
int main(){
    int array[]={-2,3,11,9,4,10,8,18};
    int i,n;
    for(i=0; i<n; i++){
        if(array[i]<=array[i-1] && array[i]<=array[i+1]){
            printf("Pick number is %d\n", array[i]);
        }
    }
    return 0;
}
